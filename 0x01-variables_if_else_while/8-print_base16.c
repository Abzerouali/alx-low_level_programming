#include <stdio.h>
/**
 * main - entry point
 * Description: all the numbers of base 16 in lowercase
 * Return: 0
 */
int main(void)
{
		int N, A;

		while (N = 0; N <= 9)
		{
			putchar(N);
			N++;
		}
		while (A = a ; A < 'g')
		{
			putchar(A);
			A++;
		}
		putchar('\n');
		return (0);
}
